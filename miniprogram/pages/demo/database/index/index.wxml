<van-button plain type="primary" bind:click="test">测试</van-button>
<van-card wx:for="{{products}}" wx:key="_id" price="{{item.price}}" desc="{{item.red}}" title="{{item.title}}" thumb="{{item.image}}" />