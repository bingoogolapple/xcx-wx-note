<van-cell-group>
  <van-cell title="{{task.title}}" value="{{task.status === 'in-progress' ? '进行中':'已完成'}}" border="{{false}}" />
</van-cell-group>
<image wx:if="{{task.image}}" src="{{task.image}}" />